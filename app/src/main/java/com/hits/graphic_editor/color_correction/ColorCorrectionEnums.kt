package com.hits.graphic_editor.color_correction

enum class RGBMode {
    RED, GREEN, BLUE
}

enum class ColorCorrectionMode {
    FACE_DETECTION,
    INVERSION,
    GRAYSCALE,
    BLACK_AND_WHITE,
    SEPIA,
    CONTRAST,
    RGB,
    MOSAIC,
    GRAIN,
    CHANNEL_SHIFT
}